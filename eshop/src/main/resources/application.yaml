spring:
  application:
    name: eshop
  main:
    banner-mode: console
  datasource:
    url: jdbc:h2:mem:eshop_db
    driver-class-name: org.h2.Driver
    username: sa
    password: password
logging:
  level:
    lt:
      codeacademy:
        spring2025:
          eshop:
            product:
              config: debug
